# weirdns

DNS mangler for custom DNS64 behaviours. No caching, only stateless forward and packet synthesis.

Tries to be as lightweight as possible (while not reinventing too many wheels) so it can run on OpenWrt routers
